import React from 'react';
import { View, Text } from 'react-native';

const AlarmListScreen = ({navigator}) => {
    return (
        <View>
            <Text>Alarm List Screen</Text>
        </View>
    );
};

export default AlarmListScreen;